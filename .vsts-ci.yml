phases:
- phase: Windows
  queue: Hosted VS2017
    timeoutInMinutes: 120
  steps:
  - template: Build/steps.yml
    parameters:
      os: 'Windows'

- phase: Linux
  queue: Hosted Linux Preview
    timeoutInMinutes: 120
  steps:
  - template: Build/steps.yml
    parameters:
      os: 'Linux'

- phase: macOS
  queue: Hosted macOS Preview
    timeoutInMinutes: 120
  steps:
  - template: Build/steps.yml
    parameters:
      os: 'macOS'
