language: csharp

solution: IronPython.sln

before_install:
  - sudo apt-get install -y nunit-console
  #- sudo apt-get install -y nunit3-console

# Should be done by default
#before_script:
#  - nuget restore IronPython.sln

script:
  - make
  #- mono ./testrunner/NUnit.Runners.*/tools/nunit-console.exe bin/v2Debug/IronPythonTest.dll

after_script:
  - nunit-console bin/v2Debug/IronPythonTest.dll

notifications:
  email:
    recipients:
      #- slide.o.mix@gmail.com
      - ticotimo@gmail.com
    on_success: always # default: change
    #on_failure: [always|never|change] # default: always
